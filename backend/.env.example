# Database
# Doit correspondre à backend/docker-compose.yml (port 5433, user postgres, password juveniste, DB note_app)
DATABASE_URL="postgresql://postgres:juveniste@localhost:5433/note_app?schema=public"

# JWT
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"
JWT_EXPIRES_IN="7d"

# Server
PORT=3000
NODE_ENV="development"

# CORS
CORS_ORIGIN="http://localhost:4200,http://localhost:8100,capacitor://localhost,http://localhost"
